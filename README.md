# 卒業研究

## 現在のタスク
- 卒研のスライドとレジュメの作成


# やったこと

## 画像サイズ64×64のデータ拡張なし
- 64×64のグラフ表示画像を使って37個のウイロイドにラベルを付けて予測
  - 3class分類で一番良かったハイパーパラメータのパターンは*重みあり*,*1e-4*,*2値画像*のとき
  - 2class分類で一番良かったハイパーパラメータのパターンは*重みなし*,*5e-5*,*2値画像*のとき
## 画像サイズ64×64のデータ拡張あり
- 今回のようなグラフ表示画像は反転したり拡大縮小などをすると生体情報が壊れるので普通のデータ拡張はできない
- ウイロイドは環状RNAであることに着目して始点を1塩基ずつずらしたグラフ表示画像を作成することでウイロイドの配列長分のデータ拡張ができる。
  - 結果37×350個ほどのデータ拡張に成功
  
- 水増しデータを含めた64×64のグラフ表示画像を使って37個のラベルありデータを予測
  - 3class分類で1番良かったハイパーパラメータのパターンは*重みあり*,*1e-4*,*2値画像*のとき
  - 2class分類で1番良かったハイパーパラメータのパターンは*重みあり*,*1e-4*,*5e-5*,*2値画像*のとき

## データ拡張なしと拡張ありでの結果の違い
 - 3class分類では多少の違いではあるがデータ拡張しなかった方がAccuracy,macro-F1ともに高かった
   - データ拡張をした方はすべての結果を比較したときAccuracy,macro-F1が低かった。
   - 混同行列を見てもデータ拡張をした方はmildとsevereがmoderateに誤分類されているパターンが多かった
   - 対してデータ拡張をしていない方の混同行列はmoderateがmild,severeに誤分類されることが多かった
   - 先行研究の結果を確認したところ教師なし学習ではmoderateは8個あったのだが1つもmoderateに分類されることなく1個がmildで7個がsevereに分類されていた
     - つまりデータ拡張をしたことでクラス間の境界が曖昧になったことで精度が落ちていると考える
     - accuracyとmacro-F1が多少良いだけだがそうなるとデータを拡張しなかった方が良い間違え方(moderateをsevere,mildに分類している)をしているといえる

 - 2class分類ではデータ拡張をした方がAccuracy,macro-F1ともに高かった
   - 先行研究の結果からmoderateをmild1個,severe7個した
   - データ拡張をしたことでより特徴を捉えることができたので2classだと精度がかなり改善できたのだと考える。
     - つまり分類においてmoderateがノイズになっている可能性が高い 

 ## データについて
- グラフ表示画像はPSTVd300.faと以下のコードを使用して作成する
   1. **make_dat.py**
   2. **dat_to_image.py**
- 重みを付けたグラフ表示画を作成したい場合はPSTVd300.faに以下のコードを使用する
   1. **count_ATGC.py**
   2. **weight_3mer.py**
   3. **weight_dat.py**
   4. **dat_to_image.py**
- なおdat_to_imageでdraw_img_gray.pyを呼び出し実行する。draw_img_grayのほうで2値画像とグレースケール画像を切り替えて作成できる
- 機械学習に使用するコードは
  - データ拡張あり64×64で
    1. **tensorflow_CNN_data_pbm(pgm).jpynb**
    2. **tensorflow_CNN_data_2class_pbm(pgm).jpynb**
 - データ拡張した64×64の結果は350_result
-jpynbにはジャックナイフ法とモデル構図が実装されているので適宜変える

## 今後の課題について
- 先行研究では学習時にmoderateを抜いてmildとsevereだけで学習してmoderateは予測に使われていた。一度自分の方でもmoderateを学習かあ抜いてmildとsevereのみでジャックナイフを実行してみて繁華性能の評価をして最終的にラベルなしデータの予測をしてみる。なお先行研究が2クラスに分類されていたのはこれが原因の可能性もある。
- この研究では3塩基の出現頻度に基づいた重みを適用してグラフ表示画像の部分的な特徴を表現していた。次は、DNA言語モデルによって文脈を考慮した多次元ベクトルを得てPCAやt-SNEなどで次元圧縮して2次元に落としてグラフ表示画像を作成してみる方法を検討している。
- 先ほどのDNA言語モデルの予測をCNNで実行したうえである程度の精度が見込めた場合はVITで分類して精度が改善するかどうかを確認をする。
